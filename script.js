const menuButton=document.querySelector('.menu-button');const menu=document.querySelector('#hovudmeny');menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));menu.classList.toggle('open',!open)});menu?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{menu.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));document.querySelector('#year').textContent=new Date().getFullYear();
const form=document.querySelector('#kontakt-skjema');form?.addEventListener('submit',async event=>{event.preventDefault();const status=form.querySelector('.form-status');form.querySelectorAll('.is-invalid').forEach(el=>{el.classList.remove('is-invalid');el.removeAttribute('aria-invalid')});if(!form.checkValidity()){const invalidFields=form.querySelectorAll(':invalid');invalidFields.forEach(el=>{el.classList.add('is-invalid');el.setAttribute('aria-invalid','true')});status.textContent='Kontroller dei markerte felta før du sender.';form.querySelector(':invalid')?.focus();return}const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Sender …';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Kunne ikkje sende');form.reset();status.textContent='Takk! Førespurnaden er send. Bjørn tek kontakt så snart han kan.';status.focus()}catch(error){status.textContent='Noko gjekk gale. Prøv igjen, eller kontakt Bjørn direkte på telefon eller e-post.';status.focus()}finally{button.disabled=false;button.textContent='Send førespurnad'}});
